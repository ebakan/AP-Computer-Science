package fraction;
/**
 * FractionDemo.java
 *
 * Fraction_public_private_demo application
 *
 * Mr. Casey McCullough
 * 11/15/09
 */
 
public class FractionTester {
    
    public static void main(String[] args) throws DenominatorEqualsZeroException {
     Fraction f = new Fraction(81,18);
     f.newDenominator(0);
     /*
     DecimalFormat df = new DecimalFormat ("#.###");
     
     Fraction f1 = new Fraction(2, 3);
     Fraction f2 = new Fraction (3, 5);
     Fraction f3 = new Fraction (-2, -9);
     Fraction f4;
     Fraction f = new Fraction(1,0);
     
        System.out.println ("Fraction 1: " + f1); // if toString() is defined correctly, this will work
        System.out.println ("Fraction 2: " + f2); 
        System.out.println ("Fraction 3: " + f3); 
        
     System.out.println (f1 + " has a decimal equivalent of: " + df.format(f1.toDouble()));
     System.out.println (f2 + " has a decimal equivalent of: " + df.format (f2.toDouble()));
     System.out.println (f3 + " has a decimal equivalent of: " + df.format(f3.toDouble()));
     
     System.out.println ("The first numerator: " + f1.getNumerator());
     System.out.println ("The first denominator: " + f1.getDenominator());
     
     if (f1.equals(f2))
          System.out.println ("f1 and f2 are equal.");
       else
         System.out.println ("f1 and f2 are NOT equal.");

       f4 = f1.reciprocal();
       System.out.println ("The reciprocal of " + f1 + " is: " + f4);
     */

     
     
    }
}
